# Session Log — 2026-08-14 — LoRA Rerun Confirmed on Real Hardware

## Prompts (verbatim, in order)

### Prompt 1

> LoRA rerun on Mistral-7B (fixed EOS-masking collator) is confirmed. Final
> numbers from results/mis_results.csv:
>
> EXP-MIS-LORA-CNN: ROUGE1 0.2890/ROUGE2 0.1074/ROUGEL 0.1965 (baseline
> 0.2387/0.0840/0.1607, +21.1%), training_time_hrs 0.89, peak_vram_gb 6.92
>
> EXP-MIS-LORA-SQUAD: EM 85.5/F1 91.96 (baseline EM 8.0/F1 24.19, +280%),
> training_time_hrs 0.42, peak_vram_gb 6.93, inference_latency_ms 625.1
> (down from 9327.6 pre-fix - model now stops at EOS instead of running to
> max_new_tokens)
>
> 1. Replace the NEEDS RERUN rows in logs/experiment_tracking.csv with these
>    confirmed final numbers, referencing results/mis_results.csv.
>
> 2. Update PROJECT_STATE.md: remove the blocker, mark it resolved and
>    confirmed on real hardware (not just code review) - both the EM
>    collapse fix and the debug_predictions spot-check (5 examples per
>    dataset, terminated cleanly, matching references almost exactly for
>    SQuAD).
>
> 3. Add a qualitative note (PROJECT_STATE.md or a new note in
>    EXPERIMENT_MATRIX.md, wherever fits the doc structure) for the eventual
>    report: EXP-MIS-LORA-CNN's raw predictions (logs/debug_predictions/)
>    show repetition loops and a rambling continuation style rather than the
>    terse bullet-point style of CNN/DailyMail references, despite ROUGE
>    improving - a real quality limitation of a base (non-instruction-tuned)
>    model with r=8 LoRA on 1000 examples/2 epochs, not a pipeline defect.
>    Don't overstate the ROUGE gain as "clean" summarization quality.
>
> 4. Also document precisely: EXP-MIS-LORA-SQUAD's 10x latency improvement
>    is NOT an inherent LoRA speedup - it's the EOS fix letting the model
>    stop generating instead of running to max_length every time. Make sure
>    speedup_factor isn't misread later as "LoRA makes inference 10x faster"
>    in isolation from this context.
>
> 5. Update logs/daily_standup.md's today's entry with the final confirmed
>    results and close out the NEEDS RERUN status. Log in
>    knowledge/ai-usage-log/ per convention.
>
> Commit when ready - this one's confirmed, no need to hold for review first.

## AI-Generated vs Human-Written

All doc/CSV edits this session are AI-generated. The real experiment numbers (ROUGE1/2/L, EM, F1, training_time_hrs, peak_vram_gb, inference_latency_ms) came from the engineer's Kaggle rerun and `results/mis_results.csv`, not generated by Claude Code:
- `logs/experiment_tracking.csv` — `EXP-MIS-LORA-CNN`/`EXP-MIS-LORA-SQUAD` rows changed from `NEEDS RERUN` to `CONFIRMED FINAL` with the real rerun numbers and a summary of the debug-predictions spot-check
- `PROJECT_STATE.md` — Current Blockers section rewritten: the open EOS-masking blocker replaced with a "Resolved (2026-08-14)" note confirming the fix on real hardware; Component Status Summary, GPU Budget Tracking, and What's Next (Week 2 checklist closed out, Week 3 opened) updated to match; new Session 3 entry added to Session Notes
- `EXPERIMENT_MATRIX.md` — new "Qualitative Notes for Report" section added (CNN repetition/rambling caveat, SQuAD speedup-attribution caveat)
- `logs/daily_standup.md` — new `2026-08-14 — Kaggle (LoRA rerun confirmed)` entry appended (the existing same-day entries already covered the earlier blocked/buggy-collator session, so this is a new entry for the new session rather than an edit to a prior one)
- This file

## Key Decisions

- **Read the actual `logs/debug_predictions/EXP-MIS-LORA-CNN.txt` and `EXP-MIS-LORA-SQUAD.txt` files before writing the qualitative notes**, rather than taking the engineer's summary at face value. Confirmed directly: CNN example 0 has a verbatim 3x repetition of "I'm not going to say anything about the last few weeks"; SQuAD examples show near-exact matches to references (4/5 exact, e.g. `'13,000 BP'` vs references `['13,000 BP', '13,000 BP', '13,000 BP']`) with short, cleanly-terminated answers instead of the pre-fix runs to `max_new_tokens`.
- **Put the qualitative notes in `EXPERIMENT_MATRIX.md`** (new "Qualitative Notes for Report" section, placed after Quality Gates and before Recovery Procedures) rather than `PROJECT_STATE.md`, since these are stable, report-facing findings about specific experiments' result quality — not "current reality" state that changes session to session the way `PROJECT_STATE.md`'s Blockers/GPU Budget sections do. `PROJECT_STATE.md`'s resolved-blocker note cross-references this section rather than duplicating the caveats inline, per `CLAUDE.md`'s instruction that these files shouldn't duplicate each other.
- **Worded the SQuAD speedup caveat to name the specific number** (`speedup_factor` = 10.49x in the CSV) rather than a vague "be careful," and added it in three places (experiment_tracking.csv notes, EXPERIMENT_MATRIX.md, daily_standup.md) since the engineer explicitly flagged this as a number likely to be misread later in report writing if not caught at the source.
- **Counted the discarded buggy-collator run's GPU-hours in `PROJECT_STATE.md`'s GPU Budget Tracking**, not just the confirmed rerun's — ~1.27h (0.86+0.41hr) from the earlier session plus ~1.31h (0.89+0.42hr) from this rerun, ~2.6h total against the 4h Week 2 plan. `CLAUDE.md`'s "PROJECT_STATE.md must reflect reality, not intent" makes discarding wasted-but-real GPU time from the budget tracking dishonest bookkeeping, even though only the rerun's experiment results are the ones that count as final.
- **Marked Week 2's quality gate as passed** (`EXPERIMENT_MATRIX.md`: "training time roughly 40-60% of full FT cost, no OOM") and updated `PROJECT_STATE.md`'s Current Phase to WEEK 3, since the engineer's message described this as confirmed/final with no further review requested and explicitly authorized moving straight to commit.
- **Committed at the end of this session**, per the engineer's explicit "no need to hold for review first" instruction — the only session so far in this log where a commit happened without a prior "report back, don't commit yet" round-trip.

## Experiments Executed (reported by engineer)

- `EXP-MIS-LORA-CNN` — confirmed final: training_time_hrs 0.89, peak_vram_gb 6.92, ROUGE1/2/L 0.2890/0.1074/0.1965 vs baseline 0.2387/0.0840/0.1607 (+21.1%). Real ROUGE gain, but raw predictions show repetition loops / rambling style, not clean bullet-point summarization — documented as a quality caveat, not treated as a clean win.
- `EXP-MIS-LORA-SQUAD` — confirmed final: training_time_hrs 0.42, peak_vram_gb 6.93, EM 85.5/F1 91.96 vs baseline EM 8.0/F1 24.19 (+280%), inference_latency_ms 625.1 (down from 9327.6ms pre-fix, speedup_factor 10.49). EOS-masking root cause from the previous session is now confirmed directly against real generated predictions, not just inferred from aggregate metrics.

No new experiments run this turn — this was a documentation/tracking session merging real Kaggle results the engineer already had in hand.

## Issues Encountered

None. This session closed out the `NEEDS RERUN` status opened in the previous session (`2026-08-14_lora-squad-em-collapse.md`) — the EOS-masking fix worked as diagnosed, confirmed on real hardware via both the metrics and a direct read of the raw generated predictions.
